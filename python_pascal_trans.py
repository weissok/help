print("Вывод текста") # аналог команды WriteLn
int(input()) # аналог команды readln целочисленного числа
input("Ввод значений") # аналог команды Readln
int(A)=2 #A:=2 var A: integer;
float(input()) # аналог команды readln дробного числа
str(input()) # аналог команды readln строки
while a>0:          #      repeat:
   print() # аналог команды   until
for i in range (1, 20):
   print("hell")        #20 раз вы увидите слово ад на английском, это аналог паскалевского фора (from X to Y)
len(s) #lenth(s) - подсчёт количество переменных в массиве/списке
